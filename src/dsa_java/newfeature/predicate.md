Predicate (Interface) (Single Abstract Method) 
=================================
Conditional Checks.

interface predicate<T> {
    public abstract boolean (Test t);
}

The variable used in predicate should be final.

